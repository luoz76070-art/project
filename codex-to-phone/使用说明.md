# Mobile Codex 使用说明

更新时间：2026-05-12

## 最终交付文件

最终交付目录：

`<PROJECT_ROOT>\安装文件`

压缩包：

`<PROJECT_ROOT>\安装文件.zip`

目录内容：

- `MobileCodexManager.exe`：电脑端管理器，双击这个启动。
- `MobileCodexRelay.exe`：电脑端 Relay 服务，由管理器启动。
- `.env.local`：配置文件，保存端口、Token、Codex 路径、DDNS 域名。
- `apk\mobile-codex-debug.apk`：手机端 APK。
- `docs\html\*.html`：IPv4、IPv6、Token 和排错教程。

## 电脑端启动

1. 确认电脑已安装并登录 Codex Desktop。
2. 打开 `安装文件`。
3. 双击 `MobileCodexManager.exe`。
4. 检查或修改：
   - `Host`：局域网 IPv4 用 `0.0.0.0`，IPv6/DDNSGo 用 `::`。
   - `Port`：默认 `8787`。
   - `Token/密码`：手机端必须填同一个值。
   - `CODEX_HOME`：留空时管理器会自动使用当前 Windows 用户的 `%USERPROFILE%\.codex`。
   - 默认工作目录：留空时管理器会自动使用当前 `安装文件` 目录，可在管理器里改成你的项目目录。
5. 点保存配置。
6. 点启动。
7. 使用管理器显示的 IPv4、IPv6 或 DDNS 地址填到手机 APK。

## 手机端安装

1. 把 `安装文件\apk\mobile-codex-debug.apk` 安装到手机。
2. 打开 APK。
3. 填电脑端管理器显示的地址。
4. 填 `.env.local` 里的 `MOBILE_CODEX_TOKEN`。
5. 点测试连接或刷新。

## 外网访问提醒

- IPv4 公网：需要路由器或云服务器转发 `8787` 到电脑。
- IPv6 公网：电脑 Relay 监听 `::`，防火墙放行 `8787`，DDNSGo 解析到当前 IPv6。
- 域名示例：`example.your-domain.com`。
- 如果手机流量能解析 AAAA 但连接超时，优先检查 Windows 防火墙、光猫/路由 IPv6 入站、运营商 IPv6 入站策略。

## 已实测

- `安装文件\MobileCodexManager.exe --start-minimized` 可以启动包内 Relay。
- `http://127.0.0.1:8798/health` 返回 `ok=true`。
- 返回的 `defaultCwd` 指向 `安装文件`，说明该目录可独立运行。
- 测试后无遗留 `MobileCodexRelay` 或 `MobileCodexManager` 进程。

## 重要限制

- 这个包不依赖 Node.js、pnpm、node_modules。
- 电脑仍必须安装并登录 Codex Desktop。
- 换电脑时通常只需要重新检查 `CODEX_HOME`、默认工作目录、Host、防火墙和 Token。
- 当前 exe 未做正式代码签名，Windows 可能提示未知发布者。
